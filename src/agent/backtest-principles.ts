/**
 * バックテストの原則 — Finxのナレッジベース
 *
 * wshobson/agents@backtesting-frameworks から抽出・凝縮。
 * バックテスト結果の「嘘」を見抜くための必須知識体系。
 *
 * 使いどころ:
 * - バックテストタブの戦略評価
 * - 「この戦略良さそう」と言われた時の質検証
 * - バックテスト結果をユーザーに説明する時
 *
 * 核心: バックテスト結果が良すぎる時は、まずバイアスを疑え。
 */

export const BACKTEST_PRINCIPLES = `
## バックテストの原則 — 結果を信頼するために

過去データで戦略を検証するバックテストは、**間違った使い方をすると有害**です。
「過去の好成績」を「未来の好成績」と誤認するトレーダーは、大多数が市場から退場します。
以下のバイアスと対策を常に意識してください。

### 5つの致命的バイアス

**1. Look-ahead bias（先読みバイアス）**
- 実際には取引時点で利用できないデータを使ってしまう
- 例: 「本日終値で判定→本日終値で売買」は**当日引け後でないと不可能**
- **対策:** Point-in-time データを使う。シグナル判定 → 翌営業日寄付で執行

**2. Survivorship bias（生存者バイアス）**
- 現在生き残っている銘柄のみで検証 = 倒産・上場廃止した銘柄を除外
- S&P500の現在構成銘柄で20年バックテスト = 当時のS&P500ではない
- **対策:** delisted銘柄を含めたpoint-in-timeユニバース

**3. Overfitting（過剰適合）**
- 過去データに合わせてパラメータを調整 = 未来には効かない
- **"In-sample"で最適化したパラメータは"out-of-sample"で崩壊する**
- **対策:** データを70:30で分割、30%は完全に触らず最終検証にのみ使う

**4. Selection bias（選択バイアス）**
- 100個戦略を試して成績良い3個だけ紹介 = ランダムでも成績良いものは出る
- **対策:** 戦略を事前登録、全結果を開示、統計的有意性を検定

**5. Transaction cost ignorance（取引コスト無視）**
- スプレッド、手数料、スリッページを無視 = 非現実的な成績
- 高頻度戦略ほど致命的
- **対策:**
  - 米株: 往復0.02%〜0.05%（大型） / 0.1%〜0.3%（小型・低流動性）
  - 日本株: 楽天証券 米株取引0.495% (税込)、最低手数料あり
  - レバETF（SOXL等）: 日々リバランスのdrag、長期ホールドで減価

### 信頼できるバックテストの条件

**必須:**
1. **Out-of-sample検証** — 学習期間 vs 検証期間を分ける
2. **Walk-forward analysis** — 過去N年で学習 → 次の1年で検証 → 窓を前進
3. **取引コストモデル** — 現実的な手数料/スプレッド/スリッページ
4. **複数マーケットレジーム** — 牛市/熊市/レンジ市でそれぞれ成績確認
5. **最大ドローダウン** — リターンだけでなく、最大含み損期間と深さ

**推奨:**
6. **Monte Carloシミュレーション** — 取引順序をシャッフルして成績分布確認
7. **Parameter sensitivity** — パラメータ±10%で成績が崩れない
8. **ベンチマーク比較** — S&P500やバイ&ホールド戦略を超えているか

### パフォーマンス指標の読み方

**単独では判断しない。組み合わせて判断する:**

| 指標 | 良い目安 | 注意点 |
|------|--------|-------|
| **Sharpe Ratio** | 1.0以上、1.5以上は優秀 | 単一指標を盲信しない |
| **Sortino Ratio** | 2.0以上が目安 | 下側リスクのみ評価 |
| **Max Drawdown** | -25%以内が許容 | 心理的に耐えられるか |
| **Calmar Ratio** | 1.0以上 | MaxDDに対するリターン |
| **Win Rate** | 40%以上 | 勝率より損益比が重要 |
| **Profit Factor** | 1.5以上 | 総利益/総損失 |
| **Recovery Factor** | 3以上 | 純利益/MaxDD |

**よくある罠:**
- 「勝率80%です」→ 負けトレードの損失が大きいと破綻
- 「Sharpe 3.0です」→ out-of-sampleでは確実に下がる
- 「年利100%です」→ MaxDDを見ろ。-70%なら耐えられない

### Walk-Forward Analysis（実務の標準）

**手順:**
1. 10年データを用意
2. 最初の3年で戦略パラメータを最適化（in-sample）
3. 次の1年で戦略を検証（out-of-sample）
4. 窓を1年前進、2. に戻る
5. 7つのout-of-sample期間で一貫して黒字なら信頼度高

**この手法が優れている理由:**
- 過剰適合を強く抑制
- 市場レジームが変わる中での安定性を確認
- 「過去の特定期間だけ効いた」戦略を弾ける

### Finxのバックテスト機能で気をつけること

現在のFinxのバックテストタブは単純な「期間指定バックテスト」です。
以下の点でユーザーに誤解を与えないよう注意:

1. **短期間の結果は過信しない** — 3ヶ月で+30%は再現性低い
2. **取引コストを明示** — 結果が手数料込みか別か
3. **S&P500との比較** — バイ&ホールドを超えているか
4. **MaxDDを必ず見せる** — リターンだけで誉めない
5. **サンプル数** — 10トレードしかない戦略は統計的意味がない

### ユーザーへの説明の仕方

バックテスト結果を提示する時:
- ✅ 「過去のこの期間でこの成績でしたが、未来は保証されません」
- ✅ 「MaxDDは-X%でした。この下落に耐えられるか検討してください」
- ✅ 「取引コスト込みでX%です」
- ❌ 「この戦略は+80%出てます」（無条件での推奨）
- ❌ 「勝率90%です」（損益比を言わない）

バックテストは「戦略がダメな時を発見するツール」です。
「良い戦略を見つけるツール」ではありません。
`;
